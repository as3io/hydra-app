query OrganizationSettings($input: ModelIdInput!) {
	organization(input: $input) {
		id
		keys {
			id
			value
			purpose
			description
			enabled
			createdAt
			updatedAt
			accessed
			accessedAt
		}
	}
}
